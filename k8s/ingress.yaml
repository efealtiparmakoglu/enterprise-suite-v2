apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: enterprise-suite-v2-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: enterprise-suite-v2.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: enterprise-suite-v2-service
            port:
              number: 80
